# 生产表固定入口设计

## 目标

工作区固定从已配置的生产多维表格读取任务，不再向操作员展示手动粘贴飞书文档的旧入口。

## 范围

- 从页面顶部移除“飞书需求文档”输入框、“读取并规划”按钮和旧版交付未配置提示。
- 移除该表单在浏览器端的事件绑定与模式切换代码。
- 保留下方生产表扫描、任务领取、审批、重跑、结果表交付和已运行任务恢复。

## 非目标

- 不删除后端旧文档接口，避免影响已有运行和后续兼容性。
- 不修改生产表字段、扫描条件或结果表写入逻辑。

## 验收

刷新页面后，顶部不再出现手动文档输入区域；点击“扫描任务”后，既有生产表任务流程正常工作。
